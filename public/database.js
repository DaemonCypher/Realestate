/**
 * Redirects the user to the "invalid" route.
 */
function redirectToInvalid() {
    window.location.href = "/invalid";
}

/**
 * Redirects the user to the "valid" route.
 */
function redirectToValid() {
    window.location.href = "/valid";
}
